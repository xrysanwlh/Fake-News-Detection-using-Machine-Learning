

** Unzip the compressed files **


Μπορειτε κατευθειαν να τρεξετε το model.py καθώς εχουμε ηδη φορτωμενα για δικια σας ευκολια στα GermanFakeNC_Texts.csv και GermanFakeNC_RateLength.csv τα δεδομενα των 259 URL.

Εαν θέλετε να τροφοδοτήσετε το νευρωνικό με διαφορετικά δεδομένα πρεπει να αδειάσετε τα GermanFakeNC_Texts.csv και GermanFakeNC_RateLength.csv (στο πρώτο αφήστε τον τίτλο text και στο δευτερο τον τίτλο Rating) και τρέξτε το DataExtract.py προκειμένου απο το καινούριο .json που θα του δώσετε να ξανα δημιουργήσει αυτα τα 2 .csv που χρησιμοποιούνται στο DataPreProccess το οποιο γινεται import στο model.py

Για να εκτελεστεί ο κώδικας: python3 model.py


σημαντικο*****************: πρεπει να προστεθει στον φακελο το dewiki.txt το οποιο μπορειτε να το κατεβάσετε απο το λινκ που σας βαλαμε στο εμαιλ. Ή αν εχετε κατεβασει απο το ντραιβ ολοκληρη την εργασια ολα καλα.
